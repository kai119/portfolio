const {nextui} = require("@nextui-org/theme")

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // Individual NextUI Components
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

